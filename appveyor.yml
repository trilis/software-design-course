build_script:
  - cd bash-cli && gradlew.bat -i -S --no-daemon assemble

test_script:
  - cd bash-cli && gradlew.bat -i -S --no-daemon check

cache:
  - C:\Users\appveyor\.gradle

environment:
  matrix:
  - JAVA_HOME: C:\Program Files\Java\jdk1.8.0
